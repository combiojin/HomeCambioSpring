<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<!-- 회원 목록 가져오기 -->
	<select id="memberList" resultType="com.cambio.org.member.MemberDTO">
		select * from `cambio`.`member` order by member_idx desc
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="com.cambio.org.member.MemberDTO">
		insert into `cambio`.`member`
		(member_id,member_pw,member_cpw,member_mail)
		values
		(#{member_id},#{member_pw},#{member_cpw},#{member_mail})
	</insert>
	
	<!-- 회원정보 상세 보기 -->
	<select id="viewMember" resultType="com.cambio.org.member.MemberDTO">
		select * from `cambio`.`member` where member_id=#{member_id}
	</select>
	
	<!-- 회원 정보 수정 -->
	<update id="updateMember">
		update `cambio`.`member`
		set member_pw=#{member_pw},member_cpw=#{member_cpw},member_mail=#{member_mail}
		where member_id=#{member_id}
	</update>
	
	<!-- 로그인 체크 -->
	<select id="checkLogin">
		select count(*) from `cambio`.`member`
		where member_id=#{member_id} and member_pw=#{member_pw}
	</select>
	
	<!-- 회원 삭제 -->
	<delete id="deleteMember">
		delete from `cambio`.`member`
        where member_id=#{member_id}
	</delete>
	
</mapper>